version: "3.4"

services:
  quantech:
    image: quantech
    build:
      context: web
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
